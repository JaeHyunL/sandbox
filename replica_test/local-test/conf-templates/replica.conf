# 로컬 시뮬레이션용 Redis Replica 설정
port 6379
bind 0.0.0.0 -::1
protected-mode no

daemonize no
dir /data
logfile ""

appendonly yes
appendfsync everysec
replica-read-only yes

# 고정 IP(172.28.0.10 = redis-master)를 직접 참조한다.
replicaof 172.28.0.10 6379
